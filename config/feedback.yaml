spring:
  application:
    name: "${SPRING_APPLICATION_NAME:feedback}"
  datasource:
    url: "${DB_URL}"
  cache:
    type: redis
  data:
    redis:
      host: "${REDIS_HOST:redis}"
      port: "${REDIS_PORT:6379}"
